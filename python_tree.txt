testimonios/
├─ .env
├─ .env.example
├─ .gitignore
├─ Dockerfile
├─ LICENCE
├─ Makefile
├─ pyproject.toml
├─ README.md
├─ requirements.txt
├─ requirements_test.txt                # requirements usados para usar en local
│
├─ src/
│  ├─ api/                              # 🌐 Capa API REST (FastAPI)
│  │  ├─ testimonios.py                 # Endpoint /testimonios
│  │  ├─ health.py                      # Endpoint /health
│  │  └─ middleware/
│  │      └─ error_handler.py           # Middleware global para logs y excepciones
│  │
│  ├── clients/                         # Capa destinada a cambiar a ingestion para usar la cuenta de servicios
│  │   ├── drive_client.py
│  │   ├── gdocs_client.py
│  │   └── vertex_client.py
│  │
│  ├─ docs/                             # Replantear esto ya que el archivo doc no será generado con la aplicación sino que se tomara un enlace como entrada en la api y será sobre escrito por la cuenta de servicio SA
│  │  ├─ google_docs_formatter.py       #  Devuelve requests básicos para insertar título (encabezado) y contenido.
│  │  └─ google_docs_quick.py           # Crea un Google Doc con OAuth de usuario, inserta contenido y (opcional) lo hace público.
│  │
│  ├─ domain/                           # 🧩 Entidades y schemas (Pydantic)
│  │  ├─ schemas.py                     # Modelos de entrada/salida API
│  │  └─ prompts/
│  │      ├─ transcription_prompt_english.md.j2 # Plantilla del prompt en inglés (para que ek testimonio salga en inglés)
│  │      └─ transcription_prompt_spanish.md.j2 # Plantilla del prompt en español (para que la testimonio salga en español)
│  │
│  ├─ ingestion/                        # 📥 Entrada de datos y descargas
│  │  ├─ auth.py                        # Autenticación (service account)
│  │  └─ oauth_client.py                # Descarga de archivos desde Drive
│  │
│  │
│  ├─ llm/                              # 🧠 Clientes de modelos LLM (vía Vertex). que posiblemente se sustituya en la capa de servicios
│  │  ├─ vertex_client.py               # 
│  │  └─ gemini.py                      # Punto de entrada FastAPI (Cloud Run)
│  │
│  ├─ maintenance/                      # Para limpiar la subida de archivos a GCS
│  │
│  ├─ orchestration/                    # 🔄 Orquestación de flujos
│  │  ├─ runner.py                      # Pipeline principal
│  │  └─ tasks.py                       # vacio
│  │
│  ├─ storage/                          # ☁️ Gestión de archivos si es que se suben a GCS
│  │
│  ├─ pdf/                              # sugerir un nombre para esta capa encargada de recibir pdfs en vez de Google docs
│  │
│  ├─ logging_conf.py                   # 
│  ├─ logging_conf.py                   # Configuración de logging centralizada
│  ├─ settings.py                       # Configuración global y lectura de .env
│  ├─ main.py                           # Punto de entrada
│  └─ utils/                            # Funciones auxiliares (ej. timestamps, text cleaning)
│
│
└─ credentials/
   └─ service_account.json